#include<bits/stdc++.h>
#define pb push_back
#define f(n) for(int i=0;i<n;i++)

using namespace std;

int main()
{
	ios::sync_with_stdio(0);

	int t;
	cin >> t;
	while (t--)
	{
		string s;
		cin >> s;
		int a_count = 0, b_count = 0, f = 0;
		for (auto i : s)
		{
			if (i == 'a') {
				a_count++;
			}
		}
	}
	return 0;
}