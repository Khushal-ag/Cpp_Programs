#include<bits/stdc++.h>
#define pb push_back
#define f(n) for(int i=0;i<n;i++)

using namespace std;

int main()
{
	ios::sync_with_stdio(0);

	int a, b, c;
	cin >> a >> b >> c;
	string s;
	cin >> s;
	cout << a + b + c << " " << s;

	return 0;
}